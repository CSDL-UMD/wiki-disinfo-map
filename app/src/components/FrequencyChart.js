import React from 'react';
import { Paper } from '@mui/material';

const FrequencyChart = ({ column }) => {
  return (
    <Paper>
      <p>FrequencyChart by {column}</p>
    </Paper>
  );
};

export default FrequencyChart;
